<!DOCTYPE html>
<html lang="zh-CN">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>前景色の博客</title>
<style>
    /* 保持原有的样式 */
    body {
        font-family: Arial, sans-serif;
        margin: 0;
        padding: 0;
        line-height: 1.6;
        background-color: #f4f4f4;
        color: #333;
    }
    header, footer {
        background: #35424a;
        color: #fff;
        padding: 10px 0;
        text-align: center;
    }
    header h1, footer .nav {
        margin: 0;
        font-size: 24px;
    }
    header a, footer a {
        color: #fff;
        text-decoration: none;
        margin: 0 10px;
    }
    header a:hover, footer a:hover {
        text-decoration: underline;
    }

    .background-video {
            position: absolute;
            left: 0;
            width: 100%;
            height: calc(100% - 60px); /* 留出导航栏的空间后计算高度 */
            z-index: -1; /* 将视频放在背景层 */
            overflow: hidden;
    }
 
    .background-video video {
            position: absolute;
            top: 50%;
            left: 50%;
            min-width: 100%;
            min-height: 100%;
            width: auto;
            height: auto;
            transform: translate(-50%, -50%);
            object-fit: cover;
    }
    .logo{
        padding: 40px 0;
    }
    .nav {
        display: inline-block;
    }
    
    .content {
        
        padding: 20px;
        max-width: 800px;
        margin: 20px auto;
        background: #fff;
        box-shadow: 0 0 10px rgba(0,0,0,0.1);
    }
    .categories, .tags {
        margin-top: 20px;
    }
    .categories h3, .tags h3 {
        border-bottom: 2px solid #35424a;
        display: inline-block;
        padding-bottom: 5px;
        margin-bottom: 10px;
    }
    .categories ul, .tags ul {
        list-style: none;
        padding: 0;
    }
    .categories li, .tags li {
        margin-bottom: 10px;
    }
    .categories a, .tags a {
        text-decoration: none;
        color: #35424a;
    }
    .categories a:hover, .tags a:hover {
        text-decoration: underline;
    }
</style>
</head>
<body>
    <header>
        <h1 class="logo">
            <a href="/"><%= config.title %></a>
        </h1>
        <h1 class="nav">
            <a href="/">首页</a> | 
            <a href="<%- url_for(config.archive_dir) %>">笔记</a> | 
            <a href="/about/">关于</a> |
            <a href="#">留言</a>
        </h1>
    </header>
    
    <div class="content">
       <%- body %>
<!-- 
        <div class="categories">
            <h3>分类</h3>
            <ul>
                <% site.categories.each(category => { %>
                    <li>
                        <a href="<%- url_for(category.path) %>">
                            <%- category.name %>
                        </a>
                    </li>
                <% }) %>
            </ul>
        </div>

        <div class="tags">
            <h3>标签</h3>
            <ul>
                <% site.tags.each(tag => { %>
                    <li>
                        <a href="<%- url_for(tag.path) %>">
                            <%- tag.name %>
                        </a>
                    </li>
                <% }) %>
            </ul>
        </div>
    -->
    </div>
    <footer>
        <div class="nav">
            THE END
        </div>
    </footer>
</body>
</html>